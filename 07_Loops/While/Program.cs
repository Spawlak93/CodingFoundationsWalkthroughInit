//declaring a variable to use in out loops boolean expression for exit condition.
int number = 0;

//while the value in number is lesss than or equal to 100, run the following
while(number <= 100)
{
    System.Console.WriteLine(number);
    //Increment by 5
    number += 5;
}
